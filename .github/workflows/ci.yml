# ci.yml
# Copyright © 2025 Leviftas project authors. All rights reserved.
#
# This file is part of the Leviftas project SDK.
#
# SPDX-License-Identifier: GPL-3.0-or-later
# For the full license text, see <https://www.gnu.org/licenses/gpl-3.0.html>
#
# - Author    : Frost Leo <frostleo.dev@gmail.com>
# - Created   : 2025/9/29
# - Modified  : 2025/9/29

name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.13
      uses: actions/setup-python@v4
      with:
        python-version: "3.13"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[test]"

    - name: Run tests with coverage
      run: |
        pytest --cov=src/leviftas --cov-report=xml --cov-report=term-missing --cov-fail-under=90

    - name: Upload coverage reports to Codecov
      uses: codecov/codecov-action@v5
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        file: ./coverage.xml
        fail_ci_if_error: true